Start client :
    * new terminal on client folder
    * run command :
        - npm install 
        - npm start

Start api :
    * new terminal on api folder
    * run command :
        - npm install
        - npm run server
